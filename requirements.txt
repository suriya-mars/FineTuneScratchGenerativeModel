torch>=2.0
transformers>=4.40
datasets>=2.18
accelerate>=0.29
peft>=0.10
trl>=0.8
bitsandbytes>=0.41
pyyaml>=6.0
tqdm>=4.66
openai>=1.30
python-dotenv>=1.0

# Unsloth — install separately (CUDA-version specific):
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# or for stable: pip install unsloth
